<Page
    x:Class="SmartCity.View.FriendPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartCity.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator},Path=Friend}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel>
            <Button Name="BackButton" FontFamily="Segoe MDL2 Assets" Content="&#xE00E;" FontSize="36" Command="{Binding GoToMapPageCommand}" />
            <Button Name="AddFriendButton" FontFamily="Segoe MDL2 Assets" Content="&#xE8FA;" FontSize="36" Command="{Binding GoToFriendPageCommand}" RelativePanel.AlignRightWithPanel="True" />
            <StackPanel>
                
            </StackPanel>
        </RelativePanel>
    </Grid>
</Page>
